"use client";

import { AlertTriangle, CheckCircle2, CircleDot, Clock3, Play, RefreshCw, Send, ShieldCheck, UserRound } from "lucide-react";
import { formatDistanceToNowStrict } from "date-fns";
import { ptBR } from "date-fns/locale";
import { Drawer, DrawerContent, DrawerDescription, DrawerHeader, DrawerTitle } from "@/components/ui/drawer";
import { officeScenarios } from "@/features/office/office-simulator";
import { officePhaseLabels, officeStateLabel, type OfficeAction, type OfficeSnapshot } from "@/features/office/office-domain";

type OfficeControlDrawerProps = { open: boolean; setOpen: (open: boolean) => void; snapshot: OfficeSnapshot | null; loading: boolean; error: string | null; onDispatch: (action: OfficeAction) => void; onRetry: () => void };

const phases = ["entrada", "trabalho", "decisao", "entrega"] as const;

function timeAgo(value: string) {
  try { return formatDistanceToNowStrict(new Date(value), { addSuffix: true, locale: ptBR }); } catch { return "agora"; }
}

export function OfficeControlDrawer({ open, setOpen, snapshot, loading, error, onDispatch, onRetry }: OfficeControlDrawerProps) {
  const runScenario = (actions: OfficeAction[]) => actions.forEach(onDispatch);
  const state = snapshot?.state ?? "loading";
  return <Drawer open={open} onOpenChange={setOpen}><DrawerContent scrollable className="office-drawer"><DrawerHeader><DrawerDescription><CircleDot size={14} /> Fluxo do escritório</DrawerDescription><DrawerTitle>Do pedido à entrega</DrawerTitle></DrawerHeader>{loading && <div className="platform-state loading">Carregando checkpoint…</div>}{error && <div className="platform-state error" role="alert"><AlertTriangle size={16} />{error}<button className="text-button" type="button" onClick={onRetry}>Tentar carregar novamente</button></div>}{snapshot && <><div className="office-run-heading"><div><p className="eyebrow">EXECUÇÃO · {snapshot.runId}</p><h2>{snapshot.title}</h2><p>{snapshot.brief}</p></div><span className={`office-state-pill ${state}`}><span className="status-dot" />{officeStateLabel(state)}</span></div><div className="office-stepper" aria-label="Etapas do fluxo">{phases.map((phase) => <div key={phase} className={snapshot.phase === phase ? "active" : phases.indexOf(phase) < phases.indexOf(snapshot.phase) ? "done" : ""}><span>{phases.indexOf(phase) + 1}</span><small>{officePhaseLabels[phase]}</small></div>)}</div><div className="office-next-step"><p className="eyebrow">PRÓXIMO PASSO</p><strong>{snapshot.nextStep}</strong><span>Origem: {snapshot.source} · Responsável: {snapshot.responsible}</span></div><div className="office-actions" aria-label="Ações do fluxo">{state === "empty" && <button className="primary-button" type="button" onClick={() => onDispatch({ type: "START" })}><Play size={16} /> Iniciar execução</button>}{state === "working" && <><button className="secondary-button" type="button" onClick={() => onDispatch({ type: "REQUEST_USER" })}><UserRound size={16} /> Pedir resposta</button><button className="secondary-button" type="button" onClick={() => onDispatch({ type: "REQUEST_APPROVAL" })}><ShieldCheck size={16} /> Pedir aprovação</button><button className="secondary-button" type="button" onClick={() => onDispatch({ type: "COMPLETE" })}><Send size={16} /> Registrar entrega</button><button className="secondary-button" type="button" onClick={() => onDispatch({ type: "FAIL" })}><AlertTriangle size={16} /> Simular falha</button></>}{state === "WAITING_USER" && <button className="primary-button" type="button" onClick={() => onDispatch({ type: "ANSWER_USER", answer: "Público recorrente e leads quentes" })}><UserRound size={16} /> Responder e retomar</button>}{state === "WAITING_APPROVAL" && <><button className="primary-button" type="button" onClick={() => onDispatch({ type: "APPROVE" })}><ShieldCheck size={16} /> Aprovar pacote</button><button className="secondary-button" type="button" onClick={() => onDispatch({ type: "REJECT" })}><AlertTriangle size={16} /> Pedir revisão</button></>}{state === "error" && <button className="primary-button" type="button" onClick={() => onDispatch({ type: "RETRY" })}><RefreshCw size={16} /> Retomar checkpoint</button>}{state === "success" && <button className="secondary-button" type="button" onClick={() => onDispatch({ type: "CLEAR" })}><RefreshCw size={16} /> Preparar novo fluxo</button>}</div><section className="office-scenarios"><div className="platform-section-heading"><div><p className="eyebrow">CENÁRIOS DE ACEITAÇÃO</p><h2>Estados rastreáveis</h2></div></div>{Object.entries(officeScenarios).map(([key, scenario]) => <button className="office-scenario" key={key} type="button" onClick={() => { onDispatch({ type: "CLEAR" }); runScenario(scenario.actions); }}><span><strong>{scenario.label}</strong><small>{scenario.description}</small></span><Play size={15} /></button>)}</section><section className="office-events"><div className="platform-section-heading"><div><p className="eyebrow">HISTÓRICO</p><h2>{snapshot.events.length} eventos</h2></div><span className="checkpoint-tag"><Clock3 size={13} /> checkpoint {snapshot.checkpoint ?? "aberto"}</span></div>{snapshot.events.length ? snapshot.events.map((event) => <article className="office-event" key={event.id}><div className="office-event-icon"><CheckCircle2 size={15} /></div><div><header><strong>{event.message}</strong><time>{timeAgo(event.occurredAt)}</time></header><p>{event.impact}</p><small><b>{event.source}</b> · {event.responsible} · Próximo: {event.nextStep}</small></div></article>) : <div className="platform-state empty">Ainda não há eventos. O checkpoint será criado quando a execução começar.</div>}</section><footer className="office-delivery"><div><p className="eyebrow">ENTREGA</p><strong>{snapshot.delivery.label}</strong></div><span className={snapshot.delivery.status === "ready" ? "service-ready" : "service-pending"}>{snapshot.delivery.status === "ready" ? <><CheckCircle2 size={15} /> Pronta</> : "Protegida até concluir"}</span></footer></>}</DrawerContent></Drawer>;
}
