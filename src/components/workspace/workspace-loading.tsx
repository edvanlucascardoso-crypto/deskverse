export function WorkspaceLoading() {
  return <main className="content" aria-busy="true" aria-label="Canvas do workspace"><section className="workspace-canvas"><div className="feedback-panel" role="status"><span aria-hidden="true">…</span><h2>Preparando seu canvas</h2><p>Carregando seu workspace com segurança.</p></div></section></main>;
}
